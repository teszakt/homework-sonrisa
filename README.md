# homework-sonrisa
Home assessment for Sonrisa

PLANNING STEPS
--------------
1. Understand requirements and assumptions
2. Project setup
3. Domain model
4. REST API
5. Notification abstraction (strategy)
6. Simple admin endpoints
7. Tests
8. README and architecture diagram

PROJECT SETUP
-------------
Define each of the modules and the structure of the project. Like: 
parent.pom (containing common libs with their defined versions for simplicity)
alert-api-def (API model for REST endpoints, API definitions for future OpenAPI api.yaml -> future java class generation)
alert-model (h2 db (or any other), entities, repositories, configuration)
alert-service (business logic, interface-service impl segregation, notifications, configuration, mappers for entity and dto, custom exceptions)
alert-app (runnable application, REST controllers, global exception handler (Exception.class -> can be extended later), application.properties -> port, db connection settings)

REST
|
Controller
|
Service
|
Notification
|
Sender
|
(Persistence layer)

Firstly, I initialize the modules with the essential dependencies. Then I can populate them later with Java classes/implementation.
API first approach (create API model/endpoints, then implement the business logic, iterfaces can be used to call exact method in controller, service, then implement is later)
controllers (wireframe with interface calls)
application.yml (port, database settings)
log4j2 (log4j2.xml debug/info/error levels)
domain model (API model, DTOs in service level if needed)
business logic (proper implementation)

ASSUMPTIONS
-----------
Alerts are user defined.
One alert can notify through multiple channels.
Admin can view alerts and delivery history.
Authentication is out of scope.
Event generation is simulated.
Retry policy is omitted due to time.

DOMAIN MODEL
------------
using H2 database for simplicity
User
id (PK - primary key)
name
email
slack
alerts oneToMany (alert table)
notifications oneToMany  (notification table)

Alert
id (PK)
user - manyToOne (user table)
type
criteria
channels
notifications oneToMany (notification table)

WorldEvent
id (PK)
type
message
createdAt

Notification
"The system decided the user should be notified."
id (PK)
alert manyToOne (alert table)
user manyToOne (user table)
message
createdAt
notificationStatus
deliveries oneToMany (notification_delivery)

enum NotificationStatus {
    CREATED,
    IN_PROGRESS,
    COMPLETED,
    PARTIAL_FAILURE,
    FAILED
}

NotificationDelivery
id (PK)
notification manyToOne (notification table)
channelType
notificationDeliveryStatus
sentAt
failureReason
attempts

enum DeliveryStatus {
    PENDING,
    SENT,
    FAILED,
    RETRYING
}

---
User

1

↓

*

Alert

1

↓

*

Notification

1

↓

*

NotificationDelivery

---
User

↓

many alerts

↓

many notifications

↓

each notification

↓

many deliveries

Create repositories for each entity. Then the repository can be used in service, repository methods can be added later based on business logic.

REST API
--------
RestController
contentType - application/json
RequestBody, ResponseBody and optional PathVariables/QueryParams
APIs and its model later can be generated from OpenAPI api.yaml file (openapi-generator-maven-plugin)
Separated controllers by logic/domain.
ExceptionHandlerAdvice

POST /user/add
autogenerated id
REQUEST
{
	"name": ""
}

RESPONSE
{
	"id": 1 (generated on persistence level)
}

POST /alert/add
autogenerated id
REQUEST
{
	"userId": 1 (generated on persistence level), 
	"type": "",
	"criteria": "",
	"channels": []
}

RESPONSE
{
	"id": 1
}

POST /event/add
autogenerated id
REQUEST
{
	"type": "",
	"message": ""
}

RESPONSE
{
	"id": 1 (generated on persistence level)
}

optional endpoints for upcoming features
GET /alert/all
returns all alert

GET /user/{id}
returns user by id

GET /alert/{id}
returns alert by id

DELETE /alert/{id}
deletes alert by id

GET /admin/notification

NOTIFICATION ABSTRACTION
------------------------
For simplicity users, events and alerts are created through REST endpoints.
Later events, and alerts can be handled as an event-driven way (using MQ like RabbitMQ, or Kafka).

abstract notification for future extension
NotificationChannelType [EMAIL, SLACK]
other channels (later like SMS, Teams, etc)

NotificationSender (abtract)
send() abstract method
EmailNotificationSender
SlackNotificationSender

NotificationDispatcher
for alers in alert.channels (through channel field in Alert entity)
find sender (through User database)
then sender.send()

WordEventDetected
then
AlertMatchingService (evaluates incoming WorldEvent based on existing Alerts)
NotificationRequested
then
NotificationDispatcher

SIMPLE ADMIN ENDPOINTS
----------------------
Endpoints that necessary for display and user interaction.
Another RestController
contentType - application/json
ResponseBody (RequestBody or PathVariables/QueryParams for filtering - e.g. use, world event type)

GET /admin/alerts
GET /admin/alert/{id}
GET /admin/notifications
GET /admin/users
GET /admin/user/{id}

Authentication can be implemented later (for admin purpose). 

TESTS
-----
JUnit tests (unit tests, integration tests)
By using Mockito.

Controller tests (MockMvc)
Service tests (Mockito)
Repository tests

----------
AI PROMPTS
----------
I used ChatGPT.

-what do you think about that AI task as home assessment? What's your suggestion? "We want users to be able to set up alerts so they get notified when something important happens in the world — like breaking news, market movements, natural disasters, that kind of thing. Should work for both email and Slack. Make it flexible enough that we can add more channels later. We need an admin view too."
-what does the notification domain model look like?
-what does the notification channel domain model look like?
-how would you define channels[] field in alert entity?
-how does the NotificationSender look like?
-
-


TECHNOLOGY
----------
Java 21
SpringBoot 3.5.x
Maven
H2 database for simplicity
Mapstruct
JUnit
Mockito